pub enum PrimitiveType {
    Null,
    Boolean(bool),
    Ubyte(u8),
    Ushort(u16),
    Uint(u32),
    Ulong(u64),
    Byte(i8),
    Short(i16),
    Int(i32),
    Long(i64),
    Float(f32),
    Double(f64),
    Decimal32(f32),
    Decimal64(f64),
    Char(char),
    Timestamp(u64),
    Uuid(String),
    Binary,
    String(String),
    Symbol,
    List,
    Map,
    Array,
}
